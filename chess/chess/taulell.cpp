#include "libraries.h"
#include "defines.h"
#include "taulell.h"

char taulell[TAMANYTAULELL][TAMANYTAULELL];

void generarTaulell() {

	for (int fila = 0; fila < TAMANYTAULELL; fila++)
	{
		for (int columna = 0; columna < TAMANYTAULELL; columna++)
		{
			std::cout << "x";

		}
	}
    
}
